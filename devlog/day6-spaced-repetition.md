# Day 6：简单复习权重

## 实际完成

- 新卡片权重为 `1`。
- 答错权重加 `1`，最高 `5`。
- 答对权重乘 `0.75`，最低 `0.25`。
- 按权重随机抽题，并尽量避免刚出现的卡片连续出现。
- 随机函数可注入，因此算法可以确定性测试。

## 说明

这是可解释的启发式权重，不是完整间隔重复算法。

## 本日产出

- `src/logic/spacedRepetition.js`
- `src/logic/spacedRepetition.test.js`
